# FizzBuzz tutorial with C++

# Usage

```shell
$ g++ -o out main.cc
```

```shell
$ ls

main.cc    out*
```

```shell
$ ./out
```

Since it has already been compiled, you only need `./out` is all that is needed.
